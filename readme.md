To replicate
```
$ git clone https://github.com/opsb/confusing-elm-package-error
$ cd confusing-elm-package-error
$ elm-package install
elm-package: elm-stuff/exact-dependencies.json: openBinaryFile: does not exist (No such file or directory)
```
